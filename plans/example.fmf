summary: Basic smoke test
prepare:
  how: ansible
  playbook:
  - plans/prepare.yml
execute:
  script: |
    ls -alrtF
    cat /etc/os-release
    rpm -qa|grep -i pip
    rpm -qa|grep -i tox
    tox -e container-ansible-core-2.13

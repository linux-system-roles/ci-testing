summary: Basic smoke test
#prepare:
#  how: ansible
#  playbook:
#  - plans/prepare.yml
execute:
  script: |
    ls -alrtF
    cat /etc/os-release
    yum repolist
    yum -y install python3-pip
    rpm -qa|grep -i pip
    rpm -qa|grep -i tox
    pip list
    tox -e container-ansible-core-2.13

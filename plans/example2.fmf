summary: Basic smoke test 2
prepare:
  how: ansible
  playbook:
  - plans/prepare.yml
execute:
  script: |
    ls -alrtF
    cat /etc/os-release
    rpm -qa|grep -i pip
    rpm -qa|grep -i tox
    pip list
    yum -y search ansible-core

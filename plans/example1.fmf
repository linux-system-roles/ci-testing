summary: Basic smoke test 1
prepare:
  how: ansible
  playbook:
  - plans/prepare.yml
execute:
  script: |
    . /tmp/.venv/bin/activate
    ansible -c local -i localhost, all -m setup -a "gather_subset=!all filter=ansible_distribution*"

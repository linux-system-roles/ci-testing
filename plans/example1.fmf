summary: Basic smoke test 1
prepare:
  how: ansible
  playbook:
  - plans/prepare.yml
execute:
  script: |
    ls -alrtF
    cat /etc/os-release
    rpm -qa|grep -i pip
    rpm -qa|grep -i tox
    pip list
    tox -e container-ansible-core-2.13 -- --image-name centos-9 tests/tests_default.yml
